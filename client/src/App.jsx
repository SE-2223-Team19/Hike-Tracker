import 'bootstrap/dist/css/bootstrap.min.css';
import API from './api';


function App() {

	
	/*
	Example

	return (<BrowserRouter>
		<Routes>
		  <Route path="/" element={<Layout mode="login"/>} />
		</Routes>
	  </BrowserRouter>
	);
	*/
}

function Layout(props) {

	// mode = props.mode;

	/*

	Example

	let outlet = <></>;
	if(mode === "login") {
		outlet = <LoginPage/>;
	}

	// Page returned to the browser on considering the mode specified on the routes in App component
	return outlet;	

	*/
}

export default App;
